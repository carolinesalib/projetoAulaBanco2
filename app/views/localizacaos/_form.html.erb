<%= form_for(localizacao) do |f| %>
  <% if localizacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(localizacao.errors.count, "error") %> prohibited this localizacao from being saved:</h2>

      <ul>
      <% localizacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :geolocalizacao %>
    <%= f.text_field :geolocalizacao %>
  </div>

  <div class="field">
    <%= f.label :endereco_id %>
    <%= f.text_field :endereco_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
